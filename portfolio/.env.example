# Client-side Gemini key (visible in the browser bundle).
# In Google AI Studio, restrict this key by HTTP referrer:
#   http://localhost:3000/*
#   https://dioame.github.io/*
# Uses model gemini-3.5-flash via REST (same as Google AI generateContent)
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
